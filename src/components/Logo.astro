---
const { size = "1rem" } = Astro.props;
---

<span
  style={`
    font-size: ${size};
    font-weight: 700;
    letter-spacing: 0.04em;
    color: var(--color-accent);
  `}
>
  MV<span style="color: var(--color-text);">Works</span>
</span>
